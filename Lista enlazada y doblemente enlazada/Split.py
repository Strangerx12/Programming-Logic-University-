## Dividir una ruta de directorio en partes utilizando el método split()
ruta_directorio = "C:/Usuarios/Documentos/Proyectos/Python"
partes_ruta = ruta_directorio.split("s") # Dividir la ruta por el carácter "s"

print("Partes de la ruta:", partes_ruta)
